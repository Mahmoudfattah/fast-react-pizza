// 💰 دالة لتحويل رقم إلى عملة بصيغة منسقة
// بتستخدم Intl.NumberFormat (ميزة مدمجة في JS)
// - "en" يعني التنسيق إنجليزي
// - "currency" يعني عايزينه يظهر كعملة
// - "EUR" يعني اليورو (€)
// مثال: 12.5 → €12.50
export function formatCurrency(value) {
  return new Intl.NumberFormat("en", {
    style: "currency",
    currency: "EUR",
  }).format(value);
}


// 📅 دالة لتنسيق التاريخ بشكل لطيف ومختصر
// بتاخد تاريخ كنص (dateStr) وتعرضه بصيغة:
// "اليوم - الشهر المختصر - الساعة:الدقيقة"
// - Intl.DateTimeFormat → مدمجة في JavaScript
// - "short" يعني اختصار الشهر (مثلا: Nov)
// مثال: "2025-11-18T14:00" → "18 Nov, 02:00 PM"
export function formatDate(dateStr) {
  return new Intl.DateTimeFormat("en", {
    day: "numeric",
    month: "short",
    hour: "2-digit",
    minute: "2-digit",
  }).format(new Date(dateStr));
}


// 🕒 دالة لحساب عدد الدقائق المتبقية حتى وقت معيّن
// تاخد تاريخ/وقت على شكل نص (dateStr)
// 1️⃣ تجيب الوقت الحالي بالمللي ثانية
// 2️⃣ تجيب وقت التاريخ اللي إدّيته برضه بالمللي ثانية
// 3️⃣ تطرح الوقتين من بعض لمعرفة الفرق
// 4️⃣ تقسّم الفرق على 60000 لتحويله من مللي ثانية إلى دقائق
// 5️⃣ ترجّع رقم بالدقائق (لو موجب: لسه باقي وقت – لو سالب: الوقت عَدّى)
export function calcMinutesLeft(dateStr) {
  const d1 = new Date().getTime();       // الوقت الحالي (ms)
  const d2 = new Date(dateStr).getTime(); // الوقت الهدف (ms)
  return Math.round((d2 - d1) / 60000);   // الفرق بالدقائق (مقرب لأقرب رقم)
}

